#pragma once

#include <iostream>
#include <String>
#include <vector>
#include <memory>

enum ButtonState { NORMAL, ACTIVE, PRESSED };
enum ItemType { WEAPON, CONSUMABLE, USABLE };
enum SceneType { START_SCENE, MAIN_MENU_SCENE, WOODLAND_SCENE, WOODLAND_DUNGEON_SCENE, TOWER_SCENE, TOWER_DUNGEON_SCENE };